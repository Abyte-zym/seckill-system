services:
  app:
    build: .
    ports: ["8081:8080"]
    depends_on: [redis, mysql, rocketmq]
  redis:
    image: redis:7-alpine
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: seckill
  rocketmq:
    image: apache/rocketmq:5.1.0
